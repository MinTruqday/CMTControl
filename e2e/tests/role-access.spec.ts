import { test } from '../../ui/fixtures/runtime.js';

test('AUTH-ROLE-001 @role', async ({}, testInfo) => {
  testInfo.skip(true, 'Role credentials and approved permission expectations are not configured.');
});
